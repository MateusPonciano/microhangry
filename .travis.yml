language: node_js

services:
  - docker

script:
  - docker-compose up --build

before_install:
  - sudo apt-get update
  - docker ps
  - sudo service postgresql stop

deploy:
  provider: heroku
  skip_cleanup: true
  api_key: $HEROKU_TOKEN
  app: microhangry-api
  on:
    branch: stable
